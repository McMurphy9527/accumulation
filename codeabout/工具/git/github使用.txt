1、通过注册邮箱来创建公钥与私钥，并将公钥添加到github中
	$ ssh-keygen -t rsa -C "youremail@example.com"
	回车-》默认创建地址
	回车-》默认无密码
2、github-》账号设置-》添加ssh-》复制生成好的ssh公钥
3、进入要推送的仓库 -》添加远程地址
	$ git remote add origin git@github.com:McMurphy9527/accumulation.git
	通过‘origin’来区分不同的远程分支
4、将本地分支上传到远程
	$git push -u origin master -> yes